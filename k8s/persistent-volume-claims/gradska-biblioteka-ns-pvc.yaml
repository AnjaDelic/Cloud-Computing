apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gradska-biblioteka-ns-pvc
spec:
  volumeName: gradska-biblioteka-ns-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
